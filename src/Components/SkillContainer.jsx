const SkillContainer = ({ name }) => (
  <div className="px-4 py-2 bg-blue-100 text-[#0f172a] rounded-full text-sm font-medium shadow-sm hover:scale-105 transition">
    {name}
  </div>
);

export default SkillContainer;
